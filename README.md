# 🎨 AI Stylist - Telegram Mini App

Виртуальная примерка причесок с помощью Google Gemini AI.

## ✨ Возможности

- 📸 Загрузка селфи
- 🖼️ Фото-пример прически (необязательно)
- ✍️ Описание желаемой прически
- 🤖 AI-генерация новой прически
- 📚 История генераций
- 💳 Система кредитов

## 🛠️ Технологии

**Frontend:**
- HTML5 + Vanilla JavaScript
- Telegram WebApp API
- Responsive Design

**Backend:**
- Node.js + Express
- Google Gemini 2.0 Flash
- In-memory storage (Map)

**Хостинг:**
- Railway (Backend + Static Frontend)

## 🚀 Деплой на Railway

### 1️⃣ Подготовка

#### Получите API-ключи:

**Google Gemini:**
1. Перейдите: https://aistudio.google.com/app/apikey
2. Нажмите "Create API Key"
3. Скопируйте ключ

**Telegram Bot:**
1. Найдите @BotFather в Telegram
2. Отправьте: `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен (Bot Token)

### 2️⃣ Деплой на Railway

1. **Создайте новый проект:**
   - Перейдите: https://railway.app
   - New Project → Deploy from GitHub repo
   - Выберите репозиторий `ai-stylist-app`
   - Ветка: `main`

2. **Настройка сервиса:**
   
   **Settings:**
   - Root Directory: `backend`
   - Build Command: (оставить пустым)
   - Start Command: `npm start`

3. **Переменные окружения (Variables):**
   
   Добавьте следующие переменные:

   ```bash
   GEMINI_API_KEY=ваш_gemini_api_ключ
   TELEGRAM_BOT_TOKEN=ваш_telegram_bot_token
   PORT=8080
   NODE_ENV=production
   ```

4. **Сгенерируйте домен:**
   - Settings → Networking → Generate Domain
   - Скопируйте URL (например: `https://web-production-xxxxx.up.railway.app`)

5. **Проверьте деплой:**
   - Откройте: `https://ваш-домен.railway.app/health`
   - Должно показать: `{"status":"ok","timestamp":"..."}`

### 3️⃣ Настройка Telegram Mini App

1. Найдите @BotFather в Telegram
2. Отправьте: `/mybots`
3. Выберите вашего бота
4. Bot Settings → Menu Button → Configure Menu Button
5. Введите URL:
   ```
   https://ваш-домен.railway.app
   ```
6. Название кнопки: `🎨 AI Стилист`

### 4️⃣ Тестирование

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку меню (снизу слева)
3. Mini App должно открыться!
4. Загрузите фото и проверьте генерацию

## 🔧 Локальная разработка

### Установка:

```bash
cd backend
npm install
```

### Настройка .env:

Создайте файл `backend/.env`:

```bash
GEMINI_API_KEY=ваш_gemini_api_ключ
TELEGRAM_BOT_TOKEN=ваш_telegram_bot_token
PORT=8080
NODE_ENV=development
```

### Запуск:

```bash
cd backend
npm start
```

Приложение будет доступно на: http://localhost:8080

## 📝 Структура проекта

```
ai-stylist-app/
├── index.html              # Фронтенд (standalone HTML + JS)
├── assets/
│   └── index-xxx.js        # Скомпилированный JS (если есть)
├── backend/
│   ├── src/
│   │   ├── index.js         # Главный сервер
│   │   ├── routes/
│   │   │   ├── generate.js   # AI-генерация
│   │   │   ├── credits.js    # Управление кредитами
│   │   │   └── history.js    # История
│   │   └── middleware/
│   │       └── telegram.js   # Telegram WebApp валидация
│   └── package.json
└── README.md
```

## 🐛 Решение проблем

### Кнопка "Сгенерировать" неактивна:

- Убедитесь, что загружено фото
- Проверьте, что кредиты > 0

### Ошибка при генерации:

1. Проверьте `GEMINI_API_KEY` в Railway Variables
2. Проверьте логи в Railway Deployments
3. Проверьте квоты Gemini API

### CORS ошибки:

- Откройте DevTools (F12) → Console
- Проверьте URL backend в запросах

### Telegram не открывает Mini App:

1. Проверьте URL в @BotFather
2. URL должен начинаться с `https://`
3. Попробуйте открыть URL в браузере

## 💰 Стоимость Railway

- **$5 бесплатно** каждый месяц (Hobby Plan)
- Для MVP этого достаточно!

## 🔗 Полезные ссылки

- [Railway Docs](https://docs.railway.app/)
- [Google Gemini API](https://ai.google.dev/docs)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)
- [Telegram BotFather](https://t.me/BotFather)

## 📝 Лицензия

MIT

---

Сделано с ❤️ для Telegram
